{
  "name": "luci-singbox-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:backend": "bun test tests/backend tests/parity",
    "test:ui": "vitest run --project ui",
    "test:cross": "vitest run --project cross",
    "typecheck": "tsc --noEmit -p ..",
    "typecheck:browser": "tsc --noEmit -p browser/tsconfig.json",
    "lint": "biome ci .."
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.0",
    "@types/bun": "1.2.2",
    "typescript": "5.7.3",
    "vitest": "^4.1.9"
  }
}
